import React from "react";
import styles from './NavigationBar.module.css'

const NavigationBar = props => {
    return(
        <div className={styles.NavigationBar}>
            <p>Hi, I am navigation bar!</p>
        </div>
    )
  }
  
  export default NavigationBar;